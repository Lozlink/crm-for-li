apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

group = 'expo.modules.callerid'
version = '1.0.0'

def expoModulesCorePlugin = new File(project(':expo-modules-core').projectDir.absolutePath, 'ExpoModulesCorePlugin.gradle')
apply from: expoModulesCorePlugin
applyKotlinExpoModulesCorePlugin()
useCoreDependencies()
useExpoPublishing()

android {
  namespace 'expo.modules.callerid'

  compileSdk safeExtGet('compileSdkVersion', 35)

  defaultConfig {
    minSdk safeExtGet('minSdkVersion', 24)
    targetSdk safeExtGet('targetSdkVersion', 35)
    versionCode 1
    versionName '1.0.0'
  }

  lintOptions {
    abortOnError false
  }

  // NOTE: do NOT add a `publishing { singleVariant('release') {...} }` block here.
  // `useExpoPublishing()` (called above via applyKotlinExpoModulesCorePlugin) already
  // configures the release variant for publishing. Declaring it again throws:
  //   "Using singleVariant publishing DSL multiple times to publish variant 'release'
  //    to component 'release' is not allowed."
}

dependencies {
  implementation project(':expo-modules-core')
  implementation "androidx.core:core-ktx:1.12.0"
}

def safeExtGet(prop, fallback) {
  rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}
