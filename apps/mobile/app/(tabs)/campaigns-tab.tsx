// Placeholder screen — never mounts in practice. The tab's href in
// (tabs)/_layout.tsx redirects to the top-level /campaigns route when
// this tab is pinned via the customize-tabs sheet. Same pattern as
// whiteboard-tab.tsx and dialer-tab.tsx.
export default function CampaignsTabPlaceholder() {
  return null;
}
